# In theory this project can be used without any inventory
# but if you want to provision over ssh you can do it by making a
# copy of this file and passing it with -i to ansible-playbook
# or by appending to /etc/ansible/hosts

archlinuxarm: # hosts targeted by alarm-ansible
  hosts:
    # chroot host is dynamically added at the end of first playbook
    # yourhost:
      #ansible_host: yourhost.lan
      #ansible_user: alarm
      #ansible_ssh_pass: alarm
      #ansible_python_interpreter: "/usr/bin/python"
